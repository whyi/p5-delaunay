/**
 * @jest-environment jsdom
 */

import {expect, jest, test} from '@jest/globals';
import p5 from "p5";

describe('Vector2D', () => {
    test('foobar', () => {
        expect(1).toBe(1);
    });
});