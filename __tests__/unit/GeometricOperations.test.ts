/**
 * @jest-environment jsdom
 */

import {expect, jest, test} from '@jest/globals';
import p5 from "p5";

describe('GeometricOperations', () => {
    describe("Cross product", () => {
        it ("should compute cross product only of 2D components", () => {
            
        })
    })
});